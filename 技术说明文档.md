# English Talker — 技术说明文档

## 1. Node.js 是什么？为什么要启动这个服务？

### Node.js 是什么？

Node.js 是一个**可以让 JavaScript 在服务器上运行**的程序。

通常 JavaScript 是在浏览器（Chrome、Edge 等）里运行的，比如网页上的按钮点击、动画效果等。但 Node.js 让 JavaScript 可以脱离浏览器，在电脑后台运行，用来处理文件、网络请求等服务器任务。

可以把它理解成一个"**JavaScript 的运行环境**"——就像你需要 Python 才能运行 `.py` 文件一样，你需要 Node.js 才能运行 `.js` 服务器文件。

### 为什么 English Talker 需要 Node.js Server？

项目目录下的 `server.js` 是一个 **HTTP 静态文件服务器**：

- 当你在浏览器里打开 `http://localhost:3000` 时，浏览器向这个服务请求 `index.html`
- Node.js Server 读取 `index.html` 文件内容并发送给浏览器
- 浏览器接收后渲染出你看到的页面（头像、按钮、对话界面等）

**简单说**：Node.js Server 就像一个"**文件管家**"，你浏览器要什么东西，它就从硬盘里找到并发给你。

### 为什么不能直接双击 index.html 打开？

现代浏览器出于安全考虑，**不允许网页通过 `file://` 协议访问麦克风**（Speech Recognition API）。必须通过 `http://` 或 `https://` 协议才能使用语音功能。

所以需要一个 Web 服务器把 `index.html` "托管"起来，让浏览器通过 HTTP 协议访问它，语音功能才能正常工作。

### 架构图

```
你（浏览器）                   电脑（Node.js）
    │                              │
    ├─ 访问 http://localhost:3000 ─>│
    │                              ├─ 读取 index.html
    │<─ 返回 index.html ───────────┤
    │                              │
    ├─ 点击 Start ─────────────────┤（浏览器直接调用 Web Speech API）
    │                              │
    ├─ 你说完话 ──────────────────┤
    │                              ├─ 调用 DeepSeek API
    │<─ 获取回答 ──────────────────┤
    │                              │
    ├─ 文字转语音播放 ────────────┤（浏览器直接调用 Speech Synthesis API）
    │                              │
```

**关键**：Node.js 只负责"文件托管"，真正的语音识别、AI 对话、语音播报都在**浏览器端**完成。

---

## 2. 网页端（index.html）都做了哪些事？

`index.html` 是一个**单文件应用**（所有 HTML、CSS、JavaScript 都在一个文件里），包含三个层面：

### 2.1 界面层（HTML + CSS）

| 区域 | 说明 |
|---|---|
| 顶部标题栏 | 显示标题、当前模型名称标签、重置按钮、设置按钮 |
| 头像区域 | SVG 绘制的 Luna 头像，带呼吸光晕动画 |
| 状态文字 | 显示当前状态：准备中 / 聆听中 / 思考中 / 说话中 |
| 语音预览框 | 实时显示你正在说的文字 |
| Start / Stop 按钮 | 控制对话开始和结束 |
| 分隔线 | 将控制区和对话区分隔 |
| 对话区 | 显示你和 Luna 的对话气泡 |
| 保存按钮 | 将对话导出为 .txt 文件下载 |
| 设置弹窗 | 输入 API Key、更改 URL/模型/语言等 |

### 2.2 语音层（Web Speech API）

浏览器原生提供的语音能力，**不需要额外安装任何东西**：

- **SpeechRecognition**：语音识别（把你说的声音转成文字）
  - 支持中英文（在设置里切换输入语言）
  - 持续监听，静音 2 秒后自动触发 AI 回复
  - 实时显示识别中的文字
  - 浏览器识别会话断开后 200ms 内自动重连

- **SpeechSynthesis**：语音合成（把 AI 的文字回复读出来）
  - 使用英文语音朗读 Luna 的回答
  - 控制语速 0.9x、音调 1.05x
  - 内置 8 秒超时保护，防止语音卡死

### 2.3 业务逻辑层（JavaScript）

| 功能 | 说明 |
|---|---|
| 连续对话循环 | idle → preparing → listening → processing → speaking → listening ... |
| 静音检测 | 用户停止说话 2 秒后自动发送给 AI |
| DeepSeek API 调用 | 流式（streaming）获取 AI 回复，逐字显示 |
| 对话历史管理 | 保存最近 10 轮对话上下文 |
| 语言识别 | 自动检测用户输入语言，Luna 用同语言回复 |
| 对话导出 | 生成带时间戳的 .txt 文件 |

---

## 3. 哪些改动需要重启 Node.js Server？

### 需要重启的情况

| 改动 | 原因 |
|---|---|
| 修改 `server.js` 文件 | 服务器代码本身变了 |
| 修改端口号（想用 3001 等） | 需要在启动命令中指定 |
| 修改了 `index.html` 的 HTML 结构 | 比如加了新的按钮、改了布局 |
| 改了 CSS 样式 | 字体、颜色、间距等 |
| 改了 JavaScript 逻辑 | 对话流程、API 调用等 |

### 不需要重启的情况

| 操作 | 说明 |
|---|---|
| 修改 API Key | 在网页设置弹窗里修改，保存到浏览器本地存储 |
| 切换语言（中文/英文） | 同上 |
| 修改对话模型或 API 地址 | 同上 |
| 所有带 "Start" 按钮的操作 | 都是前端行为，不需要服务端参与 |

### 快速判断法则

- 改了 `server.js` → **必须重启**
- 改了 `index.html` → **建议重启**（不重启也能生效，但不一定）
- 只在浏览器里点按钮、填表单 → **不需要重启**

快捷重启命令：

```bash
pm2 restart server
```

---

## 4. 当前使用的技术栈

| 组件 | 详情 |
|---|---|
| AI 模型 | `deepseek-v4-flash`（DeepSeek 最新模型，旧版 `deepseek-chat` 和 `deepseek-reasoner` 已弃用） |
| API 端点 | `https://api.deepseek.com/v1/chat/completions`（OpenAI 兼容格式） |
| 语音识别 | 浏览器 Web Speech API（SpeechRecognition） |
| 语音合成 | 浏览器 Web Speech API（SpeechSynthesis） |
| 服务端 | Node.js + 原生 http 模块 |
| 进程管理 | PM2（可选） |

## 5. 关于设置保存机制

网页设置中的 URL 和模型字段会保存到浏览器的 localStorage：

- 如果用户输入的值**和代码默认值相同** → 不保存，代码更新时自动跟随
- 如果用户输入的值**和代码默认值不同** → 保存到 localStorage，刷新后保留

这样当你更新代码版本时，如果没改过设置会自动使用新版默认值，不需要手动重置。

---

## 附录：相关文件说明

| 文件 | 作用 | 是否需要 Node.js |
|---|---|---|
| `index.html` | 主应用（界面 + 样式 + 逻辑） | 需要 Node.js 托管 |
| `server.js` | HTTP 静态文件服务器 | 需要 Node.js 运行 |
| `node_modules/` | 项目依赖（PM2 等） | 仅开发/部署时用到 |
