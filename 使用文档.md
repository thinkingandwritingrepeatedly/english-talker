# English Talker — 使用文档

## 目录

1. [启动服务](#1-启动服务)
2. [首次配置 API Key](#2-首次配置-api-key)
3. [开始英语对话](#3-开始英语对话)
4. [结束对话](#4-结束对话)
5. [保存对话记录](#5-保存对话记录)
6. [切换输入语言（中文/英文）](#6-切换输入语言中文英文)
7. [更换 AI 模型](#7-更换-ai-模型)
8. [重置对话](#8-重置对话)
9. [常见问题](#9-常见问题)

---

## 1. 启动服务

> **前提**：已安装 Node.js 和 PM2（如未安装 PM2，也可以用 `node server.js` 直接启动）。

打开终端（Terminal / PowerShell / CMD），执行：

```bash
cd D:/项目/english-talker
pm2 start server.js
```

> ![screenshot-placeholder](建议截图1-终端运行pm2-start.png)
> *图 1：终端中执行 pm2 start server.js 的界面*

看到输出显示 `server.js` 状态为 `online`，说明启动成功。

在浏览器地址栏输入 **http://localhost:3000** 并回车。

> ![screenshot-placeholder](建议截图2-浏览器打开空白页面.png)
> *图 2：浏览器地址栏输入 localhost:3000*

首次打开会弹出欢迎/设置弹窗（见下一步）。

---

## 2. 首次配置 API Key

English Talker 需要调用 DeepSeek 的 AI 接口，所以需要你提供 API Key。

### 2.1 获取 DeepSeek API Key

1. 打开 https://platform.deepseek.com/
2. 注册/登录账号
3. 进入 API Keys 页面
4. 点击 "Create API Key"，复制生成的 key（以 `sk-` 开头）

### 2.2 填入配置

首次打开页面会自动弹出设置窗口：

> ![screenshot-placeholder](建议截图3-设置弹窗.png)
> *图 3：首次打开时的设置弹窗*

填写以下内容：

| 字段 | 说明 | 示例 |
|---|---|---|
| **DeepSeek API Key** | 你的 API Key（必填） | `sk-xxxxxxxxxxxxxxxx` |
| **API Base URL** | 接口地址（一般不用改） | `https://api.deepseek.com/v1` |
| **Model** | 模型名称（一般不用改） | `deepseek-chat` |
| **Input Language** | 你说的语言 | `English` 或 `中文` |

填写完成后点击 **"Start →"** 或 **"Save"**。

> 如果没有立即弹出设置窗口，可以点击右上角的 ⚙ 图标手动打开。

---

## 3. 开始英语对话

### 3.1 准备

确保：
- 电脑已连接麦克风（笔记本自带或外接）
- 浏览器提示麦克风权限时，点击 **"允许"**

### 3.2 点击 Start

> ![screenshot-placeholder](建议截图4-Start按钮界面.png)
> *图 4：点击 Start 按钮前的界面*

点击中间的 **"Start"** 按钮（或按键盘 **空格键**）。

此时：
- 头像光圈变成**绿色**并快速脉动
- 状态文字变为 **"Preparing microphone"** → **"Listening"**
- 表示可以开始说话了

> ![screenshot-placeholder](建议截图5-Listening状态.png)
> *图 5：Listening 状态，绿色光圈*

### 3.3 说话

用英语对着麦克风说话。你说的文字会实时显示在按钮上方的预览框中。

> ![screenshot-placeholder](建议截图6-说话预览.png)
> *图 6：说话时文字实时显示*

### 3.4 等待 AI 自动回复

停止说话约 **1 秒后**：
- 预览框清空
- 状态变为 **"Thinking"**，头像光圈变黄色
- Luna 的回答会逐字出现在对话区

### 3.5 自动朗读

AI 回答显示完成后，**Luna 会自动朗读**：
- 状态变为 **"Speaking"**，头像光圈变紫色
- 朗读完毕后自动再次进入 Listening 状态

**整个循环自动进行**：你说 → Luna 回 → 你说 → Luna 回……

---

## 4. 结束对话

点击 **"Stop"** 按钮（或再次按键盘空格键）。

> ![screenshot-placeholder](建议截图7-Stop按钮.png)
> *图 7：对话进行中，红色 Stop 按钮*

- 按钮变回绿色的 "Start"
- 头像回到待机状态

---

## 5. 保存对话记录

对话结束后，底部会出现 **"↓ Save conversation"** 按钮：

> ![screenshot-placeholder](建议截图8-保存按钮.png)
> *图 8：对话结束后出现的保存按钮*

点击后自动下载一个 `.txt` 文件，内容格式如下：

```
✦ English Talker — Conversation Summary
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Date: 2026-05-29 14:30
  Messages: 5 rounds
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🧑 You: What's your favorite hobby?
✦ Luna: I love reading and hiking! What about you?

🧑 You: I like playing basketball.
✦ Luna: That's awesome! Do you play regularly?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Generated by English Talker
```

---

## 6. 切换输入语言（中文/英文）

点击右上角 **⚙ 设置按钮**：

> ![screenshot-placeholder](建议截图9-设置语言.png)
> *图 9：设置中的语言选择*

在 **"Input Language"** 下拉菜单中切换：

| 选项 | 效果 |
|---|---|
| **English** | 你说英文，Luna 用英文回 |
| **中文 (Chinese)** | 你说中文，Luna **仍然用英文回**（练习英语听力） |

点击 **Save** 保存。

> **提示**：切换语言后，语音识别会重新初始化，需要重新点击 Start。

---

## 7. 更换 AI 模型

在设置弹窗中修改 **"Model"** 字段。

常用的 DeepSeek 模型：

| 模型名 | 说明 |
|---|---|
| `deepseek-chat` | 默认，通用对话 |
| `deepseek-reasoner` | 深度推理，思考更久但更详细 |

修改后点击 Save 保存。

---

## 8. 重置对话

有两种方式：

1. 点击右上角 **⟳ 按钮** → 清空所有对话历史
2. 对话结束后点击底部的 **"↺ New topic"** 链接

> ![screenshot-placeholder](建议截图10-重置按钮.png)
> *图 10：右上角重置按钮和底部 New topic 链接*

---

## 9. 常见问题

### Q: 麦克风没反应？

- 确保浏览器地址栏左边有麦克风图标且已允许
- 检查其他软件是否占用了麦克风（如微信、Zoom）
- 尝试在 Chrome 或 Edge 浏览器中使用

### Q: "Speech error network"？

一般是 API Key 配置问题。去设置里检查：

1. API Key 是否正确（以 `sk-` 开头）
2. DeepSeek 账户是否还有余额

### Q: 想换端口启动？

```bash
pm2 start server.js -- 3001
# 然后访问 http://localhost:3001
```

### Q: 怎么关掉服务？

```bash
pm2 stop server     # 暂停
pm2 delete server   # 彻底删除
```

### Q: 改了界面后没生效？

重启服务：`pm2 restart server`，然后刷新浏览器页面（F5）。

### Q: 可以用手机访问吗？

在同一局域网下，查到你电脑的 IP 地址，然后在手机浏览器输入：

```
http://192.168.x.x:3000
```

> **注意**：手机需要用 Chrome 浏览器，并且语音识别功能在手机上可能受限。
